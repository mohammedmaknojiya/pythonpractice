l=[-2,2,3,-3,-4,5,1]

current_max=l[0]
global_max=l[0]
for i in range(1,len(l)):
    current_max=max(l[i],current_max+l[i])

    if current_max> global_max:
        global_max=current_max

print(global_max)

here time complexity and space complexity both are O(1)
